server:
  port: "3000"
  host: "127.0.0.1"
  mode: "release"  # debug, release, test

database:
  host: "localhost"
  port: 3306
  username: "root"
  password: "password"
  dbname: "myapp"

jwt:
  secret: "your-secret-key-change-in-production"
  expire: "24h"